import SwiftUI

extension Color {
    static let letGoPrimary = Color.blue
    static let letGoSecondary = Color.blue.opacity(0.1)
    static let letGoBackground = Color(.systemBackground)
    static let letGoText = Color(.label)
    static let letGoGray = Color(.systemGray)
}

extension Font {
    static let letGoTitle = Font.title
    static let letGoHeadline = Font.headline
    static let letGoBody = Font.body
    static let letGoCaption = Font.caption
} 